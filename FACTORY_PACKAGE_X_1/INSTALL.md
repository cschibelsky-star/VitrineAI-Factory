# FACTORY PACKAGE X.1

Enterprise Workspace Core.

Aplicar na raiz do projeto e rodar:

php artisan optimize:clear
git add .
git commit -m "FACTORY X.1 Enterprise Workspace Core"
git push origin hostgator-baseline
